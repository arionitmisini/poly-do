const utils = require('.../api/profile');

it('Shpuld show profile for this user', () => {
  var utils = utils.Error == 404;
});

it('should square a number', () => {
  var res = utils.square(3);

  if (res !== 9) {
    throw new Error(`Expected 9, but got ${res}.`);
  }
});
